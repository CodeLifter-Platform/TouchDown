- VM should contain the logical interface and management of data between UI components and the service layer
- VMs should NOT contain heavy business logic
- VM Should implement Serilog Stuructured logging
- VM must have a complete interface applied to all public methods
- VM should have its own custom exception
- VM should be shaped like 
      Interface
      Custom exception
      VM Class